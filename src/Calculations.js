function randomLaunch() {
    // select random 2nd root from 400 to 800
    const root = Math.floor(Math.random() * 400) + 400;
    // select random peak from 100 to 200
    const peak = Math.floor(Math.random() * 100) + 100;
    
}
